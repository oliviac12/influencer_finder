# Email Configuration for Zoho
SMTP_EMAIL=your-email@zoho.com
SMTP_PASSWORD=your-zoho-password
SMTP_HOST=smtp.zoho.com  # Optional - defaults to smtp.zoho.com
SMTP_PORT=587           # Optional - defaults to 587